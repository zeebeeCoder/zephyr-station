DATABASE_URL=postgres://zephyr:zephyr@localhost:5433/zephyr
# Generate a unique high-entropy production key (for example: openssl rand -hex 32).
# One shared fleet key is temporary; add per-device credentials before fleet growth.
INGEST_API_KEY=change-me-in-production
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info
NODE_ENV=development
